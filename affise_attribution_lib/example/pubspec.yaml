name: affise_attribution_lib_example
version: 1.7.15
description: Demonstrates how to use the affise_attribution_lib plugin.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: '>=3.0.6 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  affise_attribution_lib:
    # When depending on this package from a real application you should use:
    #   affise_attribution_lib: ^x.y.z
    path: ../

  affise_attribution_module_adservice:
    path: ../../affise_attribution_module_adservice

  affise_attribution_module_advertising:
    path: ../../affise_attribution_module_advertising

  affise_attribution_module_androidid:
    path: ../../affise_attribution_module_androidid

  affise_attribution_module_appsflyer:
    path: ../../affise_attribution_module_appsflyer

  affise_attribution_module_google:
    path: ../../affise_attribution_module_google

  affise_attribution_module_huawei:
    path: ../../affise_attribution_module_huawei

  affise_attribution_module_link:
    path: ../../affise_attribution_module_link

  affise_attribution_module_meta:
    path: ../../affise_attribution_module_meta

  affise_attribution_module_network:
    path: ../../affise_attribution_module_network

  affise_attribution_module_persistent:
    path: ../../affise_attribution_module_persistent

  affise_attribution_module_phone:
    path: ../../affise_attribution_module_phone

  affise_attribution_module_rustore:
    path: ../../affise_attribution_module_rustore

  affise_attribution_module_status:
    path: ../../affise_attribution_module_status

  affise_attribution_module_subscription:
    path: ../../affise_attribution_module_subscription

  affise_attribution_module_tiktok:
    path: ../../affise_attribution_module_tiktok

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  intl: ^0.18.1
  collection: ^1.17.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
